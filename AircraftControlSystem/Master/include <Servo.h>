include <Servo.h>

Servo Servo0;
Servo Servo1;

void setup()
{
	
	Servo0.attach(9);
	Servo1.attach(10);

}

void loop()
{

	int a = analogRead(A0);
	int b = analogRead(A1);

	a = map(a,0,1023,0,180);
	b = map(b,0,1023,0,180);

	Servo0.write(a);
	Servo1.write(b);

}